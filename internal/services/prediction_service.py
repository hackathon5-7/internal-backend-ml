def preprocess_data(data):
    return data


def get_prediction_result(input_data):
    """
    Обработка входных данных
    """
    prepared_data = preprocess_data(input_data)
    # prediction = model.predict(prepared_data)
    prediction = "some prediction"
    return prediction
